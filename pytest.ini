[pytest]
# Treat unregistered markers as fatal errors, stopping the test run immediately
addopts = --strict-markers
#PytestUnknownMarkWarning: Unknown pytest.mark.smokee - is this a typo?.

# Register your official markers here
markers =
    smoke: Critical path checks for deployment.
    regression: Full system validation.
